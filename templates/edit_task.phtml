<section class="edit-task-page">
    <h2>Edit Task</h2>

    <form action="edittask.php?id=<?= $task['id'] ?>" method="POST" class="edit-form">
        <div class="form-group">
            <label for="title">Title</label>
            <input type="text" id="title" name="title" value="<?= htmlspecialchars($task['title']) ?>" required>
        </div>

        <div class="form-group">
            <label for="content">Content</label>
            <textarea id="content" name="content" rows="8"><?= htmlspecialchars($task['content']) ?></textarea>
        </div>

        <div class="switches">
            <label class="switch-container">
                Urgent
                <input type="checkbox" name="urgent" <?= $task['is_urgent'] ? 'checked' : '' ?>>
                <span class="slider"></span>
            </label>

            <label class="switch-container">
                Important
                <input type="checkbox" name="important" <?= $task['is_important'] ? 'checked' : '' ?>>
                <span class="slider"></span>
            </label>
        </div>

        <button type="submit" class="btn-save">Save</button>
    </form>
</section>